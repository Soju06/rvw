## 1. Contract and implementation

- [x] 1.1 Run an offline regression check demonstrating the missing runner input.
- [x] 1.2 Add the optional string input and wire every deploy workflow job directly to it.
- [x] 1.3 Document the default, single-label limitation, and Blacksmith caller example; synchronize the main specification and context.

## 2. Verification and handoff

- [x] 2.1 Rerun the regression check, actionlint, neutrality guard, repository gates, and OpenSpec change validation.
- [x] 2.2 Inspect the scoped diff, commit locally with the requested message, and write `/tmp/rvw-runs-on-report.md`.
