## 1. Synthesis defects and presentation controls

- [x] 1.1 P1: Write failing generic/example-bound tests, then implement generic prompts and repository examples.
- [x] 1.2 P2: Write failing domain/process vocabulary regressions, then implement source-aware patterns and allowed terms.
- [x] 1.3 P3: Write failing disabled-stage tests, then implement enabled setting, disabled facts and fallback rendering.

## 2. Publication policy and App controls

- [x] 2.1 P4/P5: Write failing strict Python policy and Worker policy/channel/conclusion tests, then implement base-policy controls and mandatory terminal checks.
- [x] 2.2 P6: Write failing floor/cap/body/thread tests, then implement deterministic inline selection and publish facts.
- [x] 2.3 Extend Python/Worker schemas and shared fixtures for all optional fields and legacy defaults.

## 3. Operator output and handoff

- [x] 3.1 P7: Write failing Korean operator-output tests, then localize plan/result/thread summaries and gate errors through both catalogs.
- [x] 3.2 Synchronize main specs, context rationale, cloud README and auto policy operator documentation.
- [x] 3.3 Inspect all diffs, run every bare gate, record counts, commit with a conventional message, and write the requested report and commit log.
