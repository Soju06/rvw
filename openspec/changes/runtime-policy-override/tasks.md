## 1. Resolve the Codex model and reasoning effort from CLI or environment

- [x] 1.1 Add failing tests: `resolve_codex_runtime_policy` returns the packaged default with nothing set, prefers an explicit option over a set variable, reads `RVW_CODEX_MODEL` / `RVW_CODEX_REASONING_EFFORT` when the option is absent, strips whitespace, rejects an empty model and an off-enum effort with a message naming `--model` / `--reasoning-effort` or the variable and listing the nine values, and keeps `reasoning_summary` at `detailed`; `RuntimeSettings` defaults `model` and `reasoning_effort` and rejects empty strings; a legacy `process.json` without the keys still loads.
- [x] 1.2 Add failing CLI tests: `review`, `gate`, `stack review`, `sample`, and `adjudicate` exit 2 on a malformed option or variable before the host gate; `run` finalizes `process.json` as `invalid_configuration` with exit 2 and a detail naming the source; a valid `run` records `runtime.model` / `runtime.reasoning_effort`, appends `--model <m> --reasoning-effort <e>` after `--no-output-timeout <n>` in the canonical command, and writes `model=` / `reasoning_effort=` to `environment.txt`; every `CodexRuntime` the pipeline constructs (discovery, tool-less adjudication, agentic expanded adjudication, stack presence, sample, re-adjudication) receives the resolved policy and `usage.json` records it.
- [x] 1.3 Implement `REASONING_EFFORT_VALUES`, `CODEX_MODEL_ENV`, `REASONING_EFFORT_ENV`, and `resolve_codex_runtime_policy` in `runtime_policy.py`; add `--model` and `--reasoning-effort` to the seven commands, `_command_runtime_policy`, the `runtime_policy` keyword on `_execute_pipeline`, `_review_pipeline`, `_gate_pipeline`, `_stack_review_pipeline`, and `_adjudicate_existing_run`, and the `configuration`-stage resolution in `_run_command`; add `RuntimeSettings.model` and `RuntimeSettings.reasoning_effort`; regenerate `process.schema.json`; leave the publication rewriter on the packaged default.
- [x] 1.4 Add the runtime-contract and operation-modes deltas, update both main specs and `context.md`, run the bare gates, then commit `feat(runtime): resolve the Codex model and reasoning effort from CLI or environment`.

## 2. Pass model and reasoning effort overrides to the sandbox and spike runs

- [x] 2.1 Add failing Worker tests: `buildRvwRunInvocation` appends shell-quoted `--model` / `--reasoning-effort` after `--json` only when supplied and throws on an empty model or off-enum effort; `optionalCodexModel` / `optionalReasoningEffort` return undefined for an unset var, trim a set one, and throw `ConfigInvalidError` for empty, whitespace, or off-enum values; `requiredConfig` includes the fields only when defined; `reviewScript` renders the arguments from the config; `parseProcessResult` accepts optional `runtime.model` / `runtime.reasoning_effort`, rejects empty strings, and parses legacy envelopes; `diagnosticText` emits `runtime` with per-field null and whole-object null; `parseStartOverrides` accepts `{}`, rejects arrays, unknown keys, an empty model, and an off-enum effort; `/start` returns 400 on malformed JSON without creating a sandbox, applies body over var over default, and echoes `model` / `reasoning_effort` in the 202 body; `sandbox-auth.ts` and `spike-contract.ts` stay import-free and enum-free for the adapter-parity test.
- [x] 2.2 Implement `REASONING_EFFORT_VALUES`, `ReasoningEffort`, `isReasoningEffort`, and the `model` / `reasoningEffort` invocation fields in `sandbox-auth.ts`; the optional vars, helpers, and `RequiredConfig` fields in `config.ts`; the `policy` argument of `reviewScript` and the start-path call in `review-job.ts`; the parser and mapping fields in `review-job-contract.ts` and the check-facts `runtime` object; `parseStartOverrides` in `spike-contract.ts` and the body handling, effective-value selection, script arguments, and 202 echo in `routes.ts`; the optional bindings in `env.d.ts`; the README rows and `/start` body documentation; the `jq -n --arg` body in `drive-spike.sh`. Leave `cloud/wrangler.jsonc` unchanged.
- [x] 2.3 Add the cloud-app-platform delta, update the main spec and `context.md`, run the cloud gates (`npm test`, typecheck, Wrangler dry-runs) and `scripts/check-deployer-neutral.py`, then commit `feat(cloud): pass model and reasoning effort overrides to the sandbox and spike runs`.
