## 1. Synthesis contract and execution

- [x] 1.1 Write failing schema, fidelity, retry/fallback and runtime-boundary tests.
- [x] 1.2 Implement strict synthesis artifact/schema, prompt and bounded executor with telemetry.
- [x] 1.3 Integrate persisted inputs, pipeline/report precedence and summary facts with regression tests.

## 2. Publication

- [x] 2.1 Write failing synthesized/fallback body, inline, evidence, marker and language tests.
- [x] 2.2 Implement synthesized publication, retained body findings and neutral diagnostic placeholders.
- [x] 2.3 Add a deterministic persisted bori#1800-inspired replay and rendered comparison.

## 3. Voice and Worker contracts

- [x] 3.1 Write failing shared voice parser parity, strictness and guidance-cap tests.
- [x] 3.2 Implement Python and Worker voice parsing and defaults.
- [x] 3.3 Extend Worker contract/check fact fixtures with and without synthesis and implement passthrough.

## 4. Verification and handoff

- [x] 4.1 Synchronize main specs and context with implementation and inspect all delegated diffs.
- [x] 4.2 Run all bare Python, OpenSpec, deployer-neutral and Worker gates.
- [x] 4.3 Write the requested report, commit coherent verified changes, and print branch commit log.

## 5. Follow-up 1 (live-run defects)

- [x] 5.1 Add failing regressions for omitted, invented and mutated literals across source types and prose fields.
- [x] 5.2 Add failing Korean/English field language, retry diagnostics, prompt header and retained artifact tests.
- [x] 5.3 Implement no-mutation fidelity, source protection and per-field language validation with the explicit prompt header.
- [x] 5.4 Synchronize specs/context, run all bare gates, append the follow-up report, commit and print the branch log.

## Follow-up 2 (literal check)

- [x] 6.1 Add persisted Korean replay fixtures and literal/source regressions.
- [x] 6.2 Separate invented checks from language protection; include PR text and composed source spans in runtime and retained validation.
- [x] 6.3 Synchronize specifications, run bare gates, append report, commit and print the branch log.
