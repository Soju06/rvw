## 1. Registry and lint behavior

- [x] 1.1 Write failing regressions for root globs, brace rejection, schedule hints, scope diagnostics, exceptions, and project/base duplicates.
- [x] 1.2 Implement loader metadata, activation fixes, dispatch hint lookup, and lane lint CLI with minimal scoped edits.

## 2. Lane discipline

- [x] 2.1 Write failing packaged inventory/activation regressions; restructure and trim packaged lanes from the audit.
- [x] 2.2 Write authoring guidance and complete inventory/move/removal changelog; add one scope lint CI step.
- [x] 2.3 Refine only bori project lane Markdown, inspect the diff, and lint using this worktree.

## 3. Verification and handoff

- [x] 3.1 Synchronize main lane-registry spec/context with the verified single-file contract and delta.
- [x] 3.2 Independently inspect all implementation changes and run every bare verification gate.
- [ ] 3.3 Commit/push rvw and create a PR; make one bori lane-only commit and push its existing PR branch.
